import 'package:flutter/material.dart';

class AppColors {
  // สีน้ำเงินหลักที่ดูสดใสและเป็นมืออาชีพ
  static const Color primary = Color(0xFF1A36BB);

  // พื้นหลังสะอาดตาด้วยสีขาว
  static const Color background = Color(0xFFFFFFFF);

  // ส่วนประกอบย่อย (เช่น Card) ใช้สีขาวหรือเทาอ่อนมากเพื่อแยกเลเยอร์
  static const Color surface = Color(0xFFF5F7FF);

  // ตัวหนังสือหลักบนพื้นขาวต้องเป็นสีเข้มเพื่อให้เล่าง่าย
  static const Color textPrimary = Color(0xFF1C1C2E);

  // ตัวหนังสือรอง หรือไฮไลท์ใช้เป็นสีน้ำเงิน
  static const Color textSecondary = Color(0xFF1A36BB);

  // ช่อง Input ใช้สีเทาอ่อนเพื่อให้ดูนุ่มนวลกว่าสีดำเข้ม
  static const Color inputBackground = Color(0xFFF0F2F8);

  // สีแดงสำหรับ Error ปรับให้ดูเข้ากับโทนน้ำเงินมากขึ้น
  static const Color error = Color(0xFFD32F2F);
}
